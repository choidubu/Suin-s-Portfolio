# 서버 포트 설정
server.port=8080

# 애플리케이션 이름
spring.application.name=suin-portfolio

# Thymeleaf 설정 (HTML 템플릿 엔진)
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# 정적 리소스 설정 (CSS, JS 등)
spring.web.resources.static-locations=classpath:/static/

# H2 데이터베이스 설정 (개발용 - 간단하게 사용 가능!)
# spring.datasource.url=jdbc:h2:file:./data/portfolio
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=

# PostgreSQL 설정 (Render 외부 DB)
spring.datasource.url=jdbc:postgresql://dpg-d40sb1s9c44c73cc1ajg-a.singapore-postgres.render.com:5432/portfolio_db_d1aq
spring.datasource.username=portfolio_user
spring.datasource.password=DuEtPnvb6uYRB7ginffwDdeWnCgdQIw9
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA 설정
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true


# H2 콘솔 활성화 (개발 시 데이터 확인용)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# JPA 설정
#spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true

# 로깅 레벨 설정
logging.level.com.portfolio=INFO
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=DEBUG

# 파일 업로드 크기 제한
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# 인코딩 설정
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.enabled=true
server.servlet.encoding.force=true

# Timezone (KST)
spring.jackson.time-zone=Asia/Seoul
spring.jpa.properties.hibernate.jdbc.time_zone=Asia/Seoul
